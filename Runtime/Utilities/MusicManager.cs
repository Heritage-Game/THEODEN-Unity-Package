using UnityEngine;

public static class MusicManager
{
    
    
    /**
     private static MusicScript _script;

    private static MusicScript GetScript()
    {
        if (_script == null) _script = Object.FindObjectOfType<MusicScript>();

        return _script;
    }

    public static void Pause()
    {
        GetScript().PauseMusic();
    }

    public static void Play()
    {
        if (CommonVariables.ThemeStatus)
            GetScript().PlayMusic();
    }*/
}